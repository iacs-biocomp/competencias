import { NivComportFilterPipe } from './niv-comport-filter.pipe';

describe('NivComportFilterPipe', () => {
	it('create an instance', () => {
		const pipe = new NivComportFilterPipe();
		expect(pipe).toBeTruthy();
	});
});
